apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: rabbitmq
spec:
  ingress:
  - from:
    - podSelector:
        matchLabels:
          io.kompose.network/rabbitmq: "true"
  podSelector:
    matchLabels:
      io.kompose.network/rabbitmq: "true"
