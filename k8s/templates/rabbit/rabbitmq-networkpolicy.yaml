apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: rabbitmq
spec:
  podSelector:
    matchLabels:
      io.kompose.network/rabbitmq: "true"
  policyTypes:
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/rabbitmq: "true"
  egress:
    - to:
        - podSelector:
            matchLabels:
              io.kompose.network/rabbitmq: "true"
