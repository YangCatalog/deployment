apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: rsync
spec:
  podSelector:
    matchLabels:
      io.kompose.network/crontab: "true"
  policyTypes:
    - Egress
  ingress:
  egress:
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - port: 587
