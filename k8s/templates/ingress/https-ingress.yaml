apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: https-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            backend:
              serviceName: yc-frontend
              servicePort: 443
